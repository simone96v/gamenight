// Theme dei tavoli dei giochi di carte.
// Allineato al pattern dei quiz games: background neutro `var(--bg)`,
// nessun override di CSS custom properties — i sotto-componenti usano i
// token standard del design system (var(--surface)/text/muted/border)
// e si adattano automaticamente a light/dark mode.

export const cardTableTheme = {
  background: 'var(--bg)',
}
